{
  "version": "0.2.0",
  "configurations": [
    // To debug, make sure a *.spec.ts file is active in the editor, then run a configuration
    {
      "command": "npx nodemon src/test/node/index.ts",
      "name": "debug node test while watching",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
